Import ('env')
 
name = 'NUCADnaCompressor'
inc = env.Dir('.')
src = env.Glob('*.cpp')

CXXFLAGS = ['-Wno-long-long']
deps = ['nuca', 'QtCore','QtGui']

env.Uic4(Glob('*.ui'))
env.Replace(CXXFLAGS=CXXFLAGS, CFLAGS=CXXFLAGS)
env.CreateProgram(name, inc, src, deps)

